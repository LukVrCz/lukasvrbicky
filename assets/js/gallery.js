let imgs=[...document.querySelectorAll('[data-lightbox]')],idx=0,last;const lb=document.querySelector('[data-lightbox-modal]');function show(i){idx=(i+imgs.length)%imgs.length;lb.querySelector('img').src=imgs[idx].dataset.src;lb.querySelector('img').alt=imgs[idx].getAttribute('aria-label')||'Fotografie'}function closeLb(){lb.classList.remove('is-open');document.body.classList.remove('modal-open');last&&last.focus()}imgs.forEach((b,i)=>b.addEventListener('click',()=>{last=b;show(i);lb.classList.add('is-open');document.body.classList.add('modal-open');lb.querySelector('[data-close]').focus()}));lb&&lb.addEventListener('click',e=>{if(e.target===lb||e.target.hasAttribute('data-close'))closeLb();if(e.target.hasAttribute('data-prev'))show(idx-1);if(e.target.hasAttribute('data-next'))show(idx+1)});document.addEventListener('keydown',e=>{if(!lb||!lb.classList.contains('is-open'))return;if(e.key==='Escape')closeLb();if(e.key==='ArrowLeft')show(idx-1);if(e.key==='ArrowRight')show(idx+1)});
