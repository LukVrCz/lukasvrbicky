
// Lokality k pozdějšímu doplnění: {name, village, region, image, related}
const LOCATIONS_DATA=[{name:"Název místa",village:"Obec",region:"Kraj",image:"/assets/images/placeholders/location.svg",related:"Související realizace"}];
(function(){const h=document.querySelector('.site-header'),t=document.querySelector('.menu-toggle'),n=document.querySelector('.nav-links');const close=()=>{n&&n.classList.remove('is-open');t&&t.setAttribute('aria-expanded','false')};addEventListener('scroll',()=>h&&h.classList.toggle('is-scrolled',scrollY>20),{passive:true});t&&t.addEventListener('click',()=>{const o=!n.classList.contains('is-open');n.classList.toggle('is-open',o);t.setAttribute('aria-expanded',String(o))});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});n&&n.addEventListener('click',e=>{if(e.target.tagName==='A')close()});const io='IntersectionObserver'in window&&new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('is-visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io?io.observe(el):el.classList.add('is-visible'));const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;let ticking=false;function px(){if(reduce)return;document.querySelectorAll('[data-parallax]').forEach(el=>{const r=el.getBoundingClientRect();if(r.bottom>0&&r.top<innerHeight){el.style.transform=`translate3d(0,${r.top*-0.035}px,0)`}});ticking=false}addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(px);ticking=true}},{passive:true});const loc=document.querySelector('[data-locations]');if(loc){loc.innerHTML=LOCATIONS_DATA.map(x=>`<article class="card"><div class="image-frame"><div class="ph"></div></div><p class="number">${x.region}</p><h3>${x.name}</h3><p>${x.village}</p><p>${x.related}</p></article>`).join('')}})();
