let lastFocus;const modal=document.querySelector('[data-video-modal]');function closeVideo(){if(!modal)return;modal.classList.remove('is-open');modal.querySelector('.video-shell').innerHTML='';document.body.classList.remove('modal-open');lastFocus&&lastFocus.focus()}document.querySelectorAll('[data-youtube]').forEach(btn=>btn.addEventListener('click',()=>{lastFocus=btn;const id=btn.dataset.youtube;modal.querySelector('.video-shell').innerHTML=`<iframe src="https://www.youtube-nocookie.com/embed/${id}?autoplay=1&playsinline=1" title="${btn.dataset.title||'Video'}" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe>`;modal.classList.add('is-open');document.body.classList.add('modal-open');modal.querySelector('[data-close]').focus()}));modal&&modal.addEventListener('click',e=>{if(e.target===modal||e.target.hasAttribute('data-close'))closeVideo()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeVideo()});
